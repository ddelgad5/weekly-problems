{
  "name": "weekly02",
  "version": "1.0.0",
  "description": "For this week’s weekly problem, you’re going to hit the GitHub API again --  but from the server side using Node.js. Your task is to access your own GitHub profile via the API, and write the raw JSON response to a file called `response.json` here in your `week-02/` directory.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ddelgad5/weekly-problems/tree/master/week-02"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.87.0",
    "request-promise-native": "^1.0.5"
  }
}
